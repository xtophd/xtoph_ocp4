_debug:            False
_cleanup:          True
_throttle:         "3"
_tmp_dir:          "{{ xtoph_vars.tmp_dir }}"

_bastion_profile:  "--required--"
_ocp_profile:      "--required--"
_network_profile:  "--required--"
_rhcos_profile:    "--required--"

_cluster_bastion:     "{{ groups['myBastion']   }}"
_cluster_bootstrap:   "{{ (groups['myBootstrap'] + groups['myMasters'])[0] | split }}"
_cluster_masters:     "{{ groups['myMasters']   }}"
_cluster_workers:     "{{ groups['myWorkers']   }}"
_cluster_sno:         "{{ groups['mySNO']   }}"

